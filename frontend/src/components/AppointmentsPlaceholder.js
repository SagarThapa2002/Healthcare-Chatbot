function AppointmentsPlaceholder() {
  return (
    <div className="rounded-lg border border-border bg-surface p-6 text-center">
      <h2 className="text-base font-semibold text-text">Appointments</h2>
      <p className="mx-auto mt-2 max-w-prose text-sm text-muted">
        A dedicated appointments view - for viewing, booking, and cancelling - is coming in a
        later update. For now, you can book, view, or cancel an appointment by chatting with
        the assistant on the Chat tab.
      </p>
    </div>
  );
}

export default AppointmentsPlaceholder;
